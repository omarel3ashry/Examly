@model ExamViewModel
@{
    List<Question> questions = Model.Exam.Questions.OrderBy(e => e.Type).ToList();
}
<!DOCTYPE html>
<html>
<head>
    <title>Exam</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 10px;
        }

        th {
            background-color: #f2f2f2;
        }

        h2 {
            margin-top: 20px;
            margin-bottom: 10px;
        }

        h3 {
            margin-bottom: 5px;
        }

        input[type="radio"] {
            margin-right: 5px;
        }

        button[type="submit"] {
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

            button[type="submit"]:hover {
                background-color: darkred;
            }

        .error-message {
            color: red;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Exam</h1>
        <table>
            <tr>
                <th>Department</th>
                <th>Course Name</th>
                <th>Instructor Name</th>
                <th>Total Grade</th>
                <th>Duration</th>
            </tr>
    
                <tr>
                    <td>@Model.DepartmentCourse.Department.Name</td>
                    <td>@Model.DepartmentCourse.Course.Name</td>
                    <td>@Model.DepartmentCourse.Instructor.Name</td>
                    <td>@Model.TotalGrade</td>
                    <td>@Model.Exam.DurationInMinutes Minutes</td>
                </tr>
            
        </table>

        <h2>Answer the Questions</h2>
        <form id="examForm" method="post" asp-controller="Student" asp-action="SubmitExam">
            @for (int i = 0; i < questions.Count; i++)
            {
                <h3>@(i + 1). @questions[i].Text</h3>
                if (questions[i].Type == 2)// true or false
                {
                    @for (int j = 0; j < 2; j++)
                    {
                        <input type="radio" id="@questions[i].Choices.ElementAt(j).Id" value="@questions[i].Choices.ElementAt(j).Id" />
                        <label for="@questions[i].Choices.ElementAt(j).Id">@questions[i].Choices.ElementAt(j).Text </label>

                    }
                }
                else
                {
                    @for (int j = 0; j < 4; j++)
                    {
                        <input type="checkbox" id="@questions[i].Choices.ElementAt(j).Id" value="@questions[i].Choices.ElementAt(j).Id" />
                        <label for="@questions[i].Choices.ElementAt(j).Id">@questions[i].Choices.ElementAt(j).Text </label>
                    }
                }
                <br />
                <hr />
                


            }
            <input type="hidden" name="examId" value="@Model.Exam.Id" />
            <div class="error-message"></div>
            <button type="submit">Submit Exam</button>
        </form>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script>
        $("#examForm").submit((e)=>{        
            $("input:checked").each((index,input)=>{
                console.log(index);
                input.name = `choices[${index}].Id`;
            })
        })
        /*/ When the document is fully loaded
        $(document).ready(function () {
            // When the exam form is submitted
            $('#examForm').submit(function (event) {
                var unanswered = false; // Flag to track if any question is unanswered

                // Check if all questions are answered
                $('input[type="radio"]').each(function () { // Iterate over each radio input
                    var questionIndex = $(this).attr('name').split('[')[1].split(']')[0]; // Extract the question index
                    var isChecked = $('input[name="Questions[' + questionIndex + '].UserAnswer"]:checked').length > 0; // Check if any radio button for this question is checked
                    if (!isChecked) { // If no radio button is checked
                        unanswered = true; // Set the unanswered flag to true
                        return false; // Exit the loop early if any question is unanswered
                    }
                });

                if (unanswered) { // If any question is unanswered
                    event.preventDefault(); // Prevent form submission
                    $('.error-message').text('Please answer all the questions.'); // Show error message
                }
            });
        });*/
    </script>
</body>
</html>
