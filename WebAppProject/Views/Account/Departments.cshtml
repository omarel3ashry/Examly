@model IEnumerable<DepartmentViewModel>
@if (Model.Count() > 0)
    foreach (DepartmentViewModel dept in Model)
    {
        <option value="@dept.Id">@dept.Name</option>
    }
else
{
    <option value="0">No Departments</option>
}
