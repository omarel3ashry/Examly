@model BranchViewModel
@{
    ViewData["Title"] = "Delete Branch - Examly";
}
<indicator hidden>admin-dash</indicator>
<div class="row justify-content-center">
    <div class="col-md-8 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <p class="card-title">Delete Branch</p>
                <div class="row">
                    <div class="col-12">
                        <h3 class="card-text"> Are you sure you want to delete <strong>@Model.Name branch?</strong></h3>
                        <form method="post" class="forms-sample mt-3">
                            <div class="form-group ">
                                <input type="submit" value="Delete" class="btn btn-danger mr-2" />
                                <a asp-action="Index" asp-controller="Manage" class="btn btn-light">Cancel</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
